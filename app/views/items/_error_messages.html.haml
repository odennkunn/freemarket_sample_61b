-# - if @item.errors.any?
-#   .alert-alert-warnig
-#     %ul
-#       - @item.errors.full_messages.each do |message|
-#         %li= message

- if item.errors.any?
  - unless item.valid?
    - if errors = item.errors.full_messages_for(column.to_sym)
      %ul
        - errors.each do |error|
          %li=error