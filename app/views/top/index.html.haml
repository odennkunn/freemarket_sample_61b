%header.header
  =render 'shared/top-header'

.header-img
  = image_tag "index_01.png"

.main-content
  .pickup-container
    .pickup-header
      %h2.pickup-header__name
        人気のカテゴリ
      .pickup-header__categoly
        %ul
          %li
            シャネル
          %li
            ルイヴィトン
          %li
            シュプリーム
  .content  
    .content__title 新着アイテム
    .view-all もっとみる>
    .content__box 
    - @items.each do |item| 
      .item 
        = link_to item_path(item.id) do
          .item__image
            = image_tag item.images.last.image.url, width: "188px", height: "188px"
          .item__text
            .item__text__name 
              = item.name
            .item__text__num  
              .item__text__num--price
                ¥ 
                = item.price
        -if item.buyer_id.present? 
          .item__sold
            .item__sold__inner
              SOLD
  

=render 'shared/top-footerimg'
.footer
  =render 'shared/top-footer'
  =link_to '出品', new_item_path, class: 'footer-self-btn' 
  =link_to new_item_path, class: 'footer-self-btn-camera' do
    = fa_icon 'camera'